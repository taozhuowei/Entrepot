```javascript
//下拉框级联
    //index: 行 level：层级(套别是0，依次递增)
    onChangeSelect(index, level) {
      //当前行数据
      const rowData = this.tableData[index]
      //当前选中项
      const curSelectedValue = rowData.model[level]
      //子列表
      let temp = []

      // //遍历当前树，根据父节点找到子节点列表
      // this.curTree.map((item) => {
      //   //匹配到元素
      //   if (item.nodeValue === curSelectedValue) {
      //     if (level === 4) {
      //       //选到单位时，读取curTypeId
      //       this.curTypeId = item.curTypeId
      //       //保存默认份数
      //       this.defaultNums = item.defaultNums
      //     }

      //     //遍历子节点数组
      //     item.children.map((child) => {
      //       temp.push(child.nodeValue)
      //     })

      //     //当前树更改为当前树的子树
      //     this.curTree = item.children
      //   }
      // })

      let res = this.deepMapTree(this.treeData, curSelectedValue)
      for (let i = 0; i < level + 1; i++) {
        res = res[0]
      }
      console.log(res)

      res.map((node) => {
        temp.push(node.nodeValue)
      })

      //赋值给select的List显示
      this.tableData[index].list[level] = temp
    },
    //深度遍历树
    deepMapTree(tree, target) {
      return tree.map((node) => {
        if (node.nodeValue === target) {
          return node.children
        }

        return this.deepMapTree(node.children, target)
      })
    },
  },
```

